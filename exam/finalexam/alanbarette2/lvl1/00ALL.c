


// -----------------------*************************-ft_putstr.c-***********************--------------------------------
#include <unistd.h>

void	ft_putchar(char c)
{
	write(1, &c, 1);
}
void	ft_putstr(char *str)
{
	int i = 0;
	
	while (str[i])
	{
		ft_putchar(str[i]);
		i++;
	}
}

// -----------------------*************************-while(xx--) -***********************--------------------------------
17-rev_print
		i = 0;
		while (av[1][i])
			i += 1;
		while (i)
			write(1, &av[1][--i], 1);
	{
		len = ft_strlen(av[1]);
		while (len--)
			write(1, &av[1][len], 1);
---------------------------------------------------------
av[1][--i] or av[1][i] i--;
---------------------------------------------------------
	while (*s)
	{
		if ((*s >= 'A' && *s <= 'Y') || (*s >= 'a' && *s <= 'y'))
			ft_putchar(*s + 1);
		else if (*s == 'Z' || *s == 'z')
			ft_putchar(*s - 25);
		else
			ft_putchar(*s);
		++s;
    }
-----------------------------------------------------------



